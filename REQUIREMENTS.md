# reports-fetcher 一期需求规格

| 项目 | 内容 |
|---|---|
| 版本 | v1.0（待确认） |
| 日期 | 2026-09-20 |
| 状态 | 待用户确认；确认后冻结为一期验收基线，变更走 [迭代计划](ITERATION_PLAN.md) §6 |
| 关联 | [架构](ARCHITECTURE.md) · [详细设计](DESIGN.md) · [HTTP 契约](HTTP_API.md) · [迭代计划](ITERATION_PLAN.md) · [评审记录](REVIEW.md) |

---

## 1. 需求来源与问题定义

**原始需求（2026-09-19）**：输入 A股/港股/美股股票代码，获取最新四个季度的财报原文，按"市场-股票代码-财报日期"归档到本地。

**评审后确认的两点演进（依据 [REVIEW.md](REVIEW.md)）**：

1. "最新四个季度"在三市场披露制度下不可直译（港股季报自愿、美股 Q4 无独立 10-Q），统一定义为"最新 N（默认 4）个**逻辑报告组**"，语义细则见 FR-3；
2. 除 CLI 外，需提供 HTTP 入口供**同一使用方的内部应用**调用（不面向第三方公网）。

一期 = 一个可恢复的财报原文获取与归档服务（CLI + HTTP）。财报内容提取、历史比较与基本面分析属于后续阶段（[ANALYSIS_ROADMAP.md](ANALYSIS_ROADMAP.md)），一期仅按 §5 保留衔接所需的元数据。

## 2. 使用方与场景

| 场景 | 使用方 | 说明 |
|---|---|---|
| S1 手动归档 | 研究者本人（CLI） | 单只或批量代码，立即得到归档文件与结果汇总 |
| S2 程序调用 | 同一使用方的内部应用（HTTP） | 异步提交任务 → 轮询 → 查询 → 下载，不阻塞调用方 |
| S3 后续分析 | 未来的分析管线 | 仅要求一期产出的 ID/checksum/元数据可稳定引用，不纳入一期验收 |

## 3. 术语约定

| 术语 | 含义 |
|---|---|
| 定期报告 | 法定/常规定期财务披露：CN 一季报/半年报/三季报/年报；HK 年报/中期报告（季报自愿）；US 10-Q/10-K/20-F |
| 逻辑报告组 | 同一（证券, 报告期, 基础类型, 报表范围）下的全部候选版本集合；`last_n` 的计数单位 |
| 报告期 / 公告日 | 报告期 = 财期期末日；公告日 = 源站披露日。两者不可互换；报告期未知时保留 null |
| 完整全文 / 摘要 / 通知 | 由 `document_role` 区分；只有可确认的完整版本（含完整修订版）可被选为归档对象 |
| report_id / artifact_id | 报告的持久身份（绑定来源去重键）/ 同一来源文件某次内容版本的身份 |
| period_source | 报告期可信级别：`source_field`（来源字段）与 `explicit_title`（标题明确期末日）、`document`（从已归档原文提取的明确期末日，仅补未知、绝不覆盖已可信期）为可信来源；`unknown` 为未知。`filing_date` 仅可作为文件名回退，绝不作 report_period 来源 |

## 4. 功能需求（FR）

> 本节是验收口径（WHAT）；行为细节与字段契约以 DESIGN.md / HTTP_API.md 为准（HOW）。

### FR-1 证券输入与市场识别

- 接受三市场代码及常见别名（前缀/后缀/大小写），规范化为 `(market, symbol)`；批量输入去重合并并保留原始别名。
- 本地规则只做识别不做有效性判定：有效性由源站 resolve 精确匹配确认；北交所等未支持能力返回 `unsupported_market_segment`，不静默落到沪深查询。
- 停牌/退市与代码不存在是不同结果；`symbol_not_found` 不得吞掉网络错误或源站风控。

### FR-2 报告发现

- 三市场默认类型：CN `Q1/H1/Q3/FY`；HK `ANNUAL/INTERIM`；US `10-Q/10-K/20-F`（修订申报归入基础类型并保留 `source_form/is_amendment`）。
- 分页与扩窗在有界预算内进行（默认回溯 ≤10 年、每证券发现请求 ≤100 次）；预算耗尽必须显式上报 `truncated`，不得伪装穷尽。
- US 历史检索不能只依赖 `filings.recent`，按需读取 `filings.files` 指向的历史文件。
- 发现失败（网络/风控/契约变化）与"无匹配报告"是不同结果；后者是正常终态（`no_reports`）。

### FR-3 报告选择（"最新 N 份"语义）

- `last_n` 默认 4、范围 1–20，指**逻辑报告组数**，不承诺覆盖 N 个财季或一个完整年度。
- 选择流程：来源类型筛选 → 排除已明确的摘要/非财报 → 识别完整修订版 → 逻辑报告分组 → 语言偏好（CN/HK 中文优先、英文回退，不全局排除英文）→ 组内选最新可确认完整版本 → 已知报告期倒序、未知期置后。
- 摘要/单独更正通知不冒充全文；仅有修订通知时可保留原全文，但必须附"未合并修订"警告。
- 未知报告期：`period_source=unknown` + null，禁止用公告日、12-31 或财季惯例猜测。
- 必须返回覆盖信息：`requested/selected`、检索范围、`exhausted/truncated`、`warnings`；不足 N 份时明确说明。

### FR-4 下载与归档

- 全部源站请求（resolve/列表/分页/重试/重定向/文件）经统一传输层：按来源组限速、有界重试（退避+抖动、尊重 Retry-After、403 不重试）、流式下载（实际字节上限）、内容与来源特征联合校验（HTTP 200 或 `<html` 不能单独证明拿到财报）。
- 归档布局 `{out}/{market}/{symbol}/` 下以报告期开头的文件名（`flat`/`nested` 可选），文件名含 `report_id/artifact_id` 保证唯一、不因标题碰撞覆盖。
- manifest 持久化完整元数据：报告期及 period_source、公告日（保留来源语义）、语言、类型、修订关系、sha256、UTC 抓取时间、警告。

### FR-5 幂等、缓存与版本

- 来源去重键 `(market, symbol, source_id)`；命中且磁盘文件校验通过 → `cached` 跳过；数据库有记录但文件缺失时按恢复规则处理，不得跳过丢失文件。
- `refresh` 显式触发重取；同来源内容变化时保留旧版本（多 artifact 并存），历史版本可按 ID 读取且 checksum 一致。
- 任何中断不产生半文件或"数据库 done 但文件无效"的脏状态。

### FR-6 失败隔离与恢复

- 单文件失败不影响同证券其他文件；单证券失败不影响整批；结果按证券/文件逐条给出稳定错误码。
- 重启后遗留状态可自动收敛（恢复规则见 DESIGN §11.3/§14），不存在永久 running。

### FR-7 CLI

- 命令：`fetch` / `list` / `serve` / `version`；参数与退出码语义见 DESIGN §12。
- `list` 联网预览元数据与覆盖信息，不下载原文（缓存副作用须说明）。

### FR-8 HTTP 服务（一期范围内后置交付，见迭代计划 I5）

- 标准链路：提交任务（202 + 幂等键）→ 轮询 → 档案查询 → 按 ID 下载；只读查询绝不隐式联网抓取。
- 鉴权：回环默认 local 模式；非回环必须令牌 + HTTPS。路径边界、ETag/304、nosniff 等安全要求按 [HTTP_API.md](HTTP_API.md)。

### FR-9 可观测性

- 日志含 request_id/job_id/report_id 关联字段；候选被排除/保留的理由可查询；不记录令牌/Cookie。

## 5. 面向后续分析的元数据要求（衔接 ANALYSIS_ROADMAP §8）

一期归档必须稳定提供：`report_id/artifact_id`、`source_url/source_id`、原文 `sha256`、UTC 抓取时间、报告期及 `period_source`、语言、申报类型（`source_form`）、修订信息（`is_amendment/revision_of`）、命名空间化的来源主体标识。未知字段保留未知，不为后续分析预填猜测值。

## 6. 非功能需求（NFR）

| # | 需求 | 验收口径 |
|---|---|---|
| NFR-1 | 限速预算：SEC 全部相关域名合并最小间隔 0.13s；CN/HK 初值 0.5s/0.3s（I0 实测修订） | 配置化；含重试/分页/重定向的全部请求计入同一来源组预算 |
| NFR-2 | 有界重试：≤4 次尝试、指数退避+抖动、尊重 Retry-After；403/契约变化不重试 | fake transport 离线单测覆盖 |
| NFR-3 | 资源上限：单文件 200 MiB、连接/读取超时、文件与任务 deadline、HTTP 请求体 64 KiB、待处理任务 ≤100 | 超限返回明确错误；不无限重试、不整文件进内存 |
| NFR-4 | 安全：默认监听 127.0.0.1；非回环强制令牌+HTTPS；文件按 ID → 受控路径映射 + 包含检查；不泄露服务器绝对路径 | HTTP_API §8 验收场景 |
| NFR-5 | 合规：仅官方公开披露来源；SEC 真实 UA 必配（未配置则 US 功能明确报错）；归档仅供同一使用方内部使用，不再分发 | README/部署说明明示 |
| NFR-6 | 环境：Docker 容器化交付为主力（镜像内 Python 3.11+），宿主机仅要求 Docker、不提供 venv 交付路径；核心依赖 requests；FastAPI/Pydantic/Uvicorn 仅 `server` 安装组；测试依赖独立 | 镜像构建 + compose 用例（probe/cli/serve）验证 |
| NFR-7 | 数据质量表达：未知即 null，不猜日期/数值；质量警告与错误码是一等公民 | FR-3/FR-6 验收包含 |
| NFR-8 | 性能：记录制目标（环境、样本量、字节、耗时、重试留档），不做无条件硬承诺；HTTP 查询不被下载任务阻塞 | I6 产出基线报告 |

## 7. 一期明确不做（负面清单）

- 财报内容提取、指标计算、基本面分析（归 ANALYSIS_ROADMAP 后续阶段）
- 6-K 业绩附件识别、40-F、北交所
- QTR-HK 默认启用（fixture 验证通过后仅作为显式可选类型）
- Webhook、任务取消、多租户、分布式队列、多实例部署
- 内置定时调度（由调用方或外部 cron 承担）
- 公网服务化与原文再分发；自动清理历史任务与旧版本

## 8. 评审意见处理结论

REVIEW.md §2/§3 的技术修正**全部采纳**（v1.1 ARCHITECTURE/DESIGN 已落实），本文档补充的只是排期与语义澄清：

| REVIEW 意见（摘要） | 处理 | 落点 |
|---|---|---|
| HTTP 交付缺失 | 采纳，纳入一期范围 | FR-8；迭代 I5 |
| HK 报告期口径矛盾/按月猜季 | 采纳，废除一切日期猜测 | FR-3；迭代 I3 |
| 更正/修订/去重规则冲突 | 采纳 document_role + 版本选择 | FR-3/FR-5；I1 框架，I2/I3 市场规则 |
| US recent 局限 / 6-K 越界承诺 | 采纳：按需读 filings.files；6-K 移出承诺入负面清单 | FR-2；迭代 I1 |
| 双写 / 文件名碰撞 / 丢失文件 | 采纳 Store 唯一提交 + ID 进文件名 + 落盘后标记 | FR-4/FR-5；I1 最小闭环，I4 完整恢复 |
| SQLite 线程归属 / 执行器 | 采纳每线程连接 + 单执行器 | I1 基础，I4/I5 完善 |
| 重试绕过限速 / 非流式 / 错误页 | 采纳统一显式传输层 | FR-4；迭代 I1 |
| Python 3.10 基线错误 | 采纳 3.11+ | NFR-6 |
| §3 各澄清项（00700 统一、类股官方映射、forms 生效、语言回退、空结果≠错误、UTC 语义、部署边界、估算撤回） | 全部采纳 | 分散至 FR/NFR 与迭代计划 |

**本方补充澄清（新增决策，非 REVIEW 原文）**：

1. **QTR-HK 启用门槛**：I3 用 fixture 验证通过前，显式请求一律 `unsupported_form`（HTTP 422 / CLI 报错）；验证通过后作为非默认显式类型启用。
2. **HTTP 归入一期但排期最后（I5）**：这是交付顺序决策，不改变其"一期必须"属性；若确认时希望更早，可与 I4 对调。
3. **工期估算不在本文承诺**：v1.0 的 2.5 人日已撤回；I0（来源契约验证）完成后在迭代计划中按迭代给出。

## 9. 需求与迭代映射

见 [ITERATION_PLAN.md](ITERATION_PLAN.md) §4 覆盖矩阵（唯一维护处）。
