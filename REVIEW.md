# 财报获取工具设计评审

日期：2026-09-20。评审对象：原 v1.0 `ARCHITECTURE.md`、`DESIGN.md`；以下章节号指原稿。当前目录只有设计文档，本次没有运行抓取器或验证巨潮、披露易的实际请求。

## 1. 结论

适配器 + 公共下载器 + SQLite/文件归档的方向合适，足以承载一期。建议保留单体、同步抓取与市场级并行；一期增加 HTTP 服务及持久化任务，不引入 Redis、Celery、微服务或向量数据库。

原稿尚不宜直接照着实现：HTTP 交付缺失，报告选择及日期口径不一致，部分代码示例与存储约束会产生错误结果。下面是文档审查发现，不代表已经修复或验证了运行代码。

## 2. 一期必须处理的问题

| 优先级 | 原文位置 | 问题及影响 | 建议 |
|---|---|---|---|
| 必须 | 架构 §1.2、§2、§11 | 把 Web 服务排除在外，无法满足其他应用调用；给 CLI 加一层同步 HTTP 包装容易因下载超时产生重复执行 | CLI/HTTP 共用应用服务；提交任务、轮询状态、查询档案、下载文件；见 [HTTP_API.md](./HTTP_API.md) |
| 必须 | 设计 §9.2、§7.5 | 港股规则一处说不假设日历年，一处把缺日期的年报设为 12-31，还按月份猜财季；会把非日历年财报归错期 | 只接受明确期末日或可审计的来源字段；缺失留空并警告，保留文件；不能按公告日冒充报告期 |
| 必须 | 设计 §6.4、§7.3、§9.4 | 一概排除更正/修订会漏掉有效完整修订版；仅按报告期去重还会误合并不同类型；“更新”排除与“更新后保留”冲突 | 区分完整修订版与独立更正通知，保留版本及来源；按报告期、类型、语言选择默认版本 |
| 必须 | 架构 §8；设计 §8、§11.4、§16.2 | US 默认不含 6-K，却承诺 ADR 自动 20-F/6-K；只看 recent 和固定三年可能取不齐 4 份年报；主文档也可能只是封面 | P0 默认只承诺 10-Q/10-K/20-F 完整主文档及可识别修订版，6-K 附件识别后续做；按需读取历史 submissions 文件，返回覆盖缺口 |
| 必须 | 设计 §10.4、§11.1–11.4 | Downloader 和 Store 各落盘一次；文件名没有 source_id；同标题碰撞可能覆盖，但数据库仍保存两条 done；只查数据库会跳过丢失文件 | Store 唯一负责提交归档；文件名带稳定 ID；文件落盘后再标记完成；跳过前检查实际文件，补充崩溃恢复规则 |
| 必须 | 设计 §11.5、§14 | `threading.Lock` 不能消除 sqlite3 默认连接的线程归属限制；HTTP 多请求会放大重复抓取和限速问题 | 每线程独立数据库连接、短事务、busy_timeout；一个持久化任务执行器，共享源站限速器；P0 单服务进程 |
| 必须 | 设计 §10.1–10.4 | urllib3 自动重试绕过外部 wait；resolve/list 也可能不经过限速；HTML 错误页同样有 `<html`；一次性读取不能约束未知长度响应 | 所有请求及重试统一走传输层；流式读取、实际字节上限、总时限；识别来源特定错误页与预期文档特征 |
| 必须 | 架构 §3；设计 §2、§3、§6、§8 | Python 3.10 与 StrEnum 不兼容；TOML 标准库解析同样要求 3.11；Report 示例接受未声明的 `report_period_inferred` | 基线改为 Python 3.11+；统一模型/DDL/API 字段名与空值语义 |
| 必须 | 设计 §7.1、§7.4 | 内层示例 JSON 是数组，伪代码按 `inner["res"]` 字典读取；固定 rowRange 也没闭合分页 | 用实际响应 fixture 定义解析器和分页，未知形态报 source_contract_changed，不能伪装空结果 |

## 3. 需要同步澄清的问题

- 港股 `0700` / `00700` 规范化不一致；统一为 `00700`。US 不应靠 1–5 个纯字母白名单拒绝类股代码；按官方 ticker 映射解析显式别名。CN 前缀规则不能替代交易所与证券类型校验；北交所作为未支持能力明确返回。
- `forms` 在部分伪代码中未使用；跨市场统一排序、过滤顺序及“不足 N 份”的表达必须固定。默认 N 是报告组数，不是财季数，也不保证覆盖一年。
- HK 全局排除 English 可能失去唯一全文；应做语言偏好和回退。业绩公告、摘要、ESG 与完整定期报告要区分。
- 无匹配报告是有效结果，HTTP 失败/响应结构变化才是来源错误；停牌或退市不能直接推导为公司不存在。缓存应有过期策略，不能因为一次空结果永久改映射。
- 公告日期应保留来源语义，抓取时间用 UTC；服务器本地时区不能决定财报日期。URL 拼接应使用解析后的 HTTPS 地址与允许域名规则。
- 原“仅个人研究、不再分发”与应用下载接口需明确部署边界：一期按同一使用方的内部应用共享归档设计；公网服务化与公开再分发不是一期承诺。
- 原 2.5 人日未计 HTTP、任务恢复、站点契约验证，不能作为交付承诺。先完成来源验证，再估算各里程碑；性能目标需记录文件总量、网络、缓存和重试条件。

## 4. 本次文档落点

- [ARCHITECTURE.md](./ARCHITECTURE.md)：一期边界、HTTP 入口、部署约束、后续分析的系统需求。
- [DESIGN.md](./DESIGN.md)：修订关键选择/归档/恢复规则，保留待实测的来源接口草案。
- [HTTP_API.md](./HTTP_API.md)：一期 HTTP 契约草案及验收场景。
- [ANALYSIS_ROADMAP.md](./ANALYSIS_ROADMAP.md)：后续内容读取、历史比较、基本面分析方案；不冻结实现和库选型。

## 5. 外部依据与验证边界

- SEC `recent` 不是完整历史，官方文档说明额外历史文件入口；后续结构化提取可使用 XBRL API，但需注意标准标签和主体级事实的覆盖边界：[SEC EDGAR APIs](https://www.sec.gov/search-filings/edgar-application-programming-interfaces)。
- SEC 限速是使用方的总请求预算，不能按域名或实例各算一份：[SEC Developer Resources](https://www.sec.gov/about/developer-resources)。
- Python 版本和连接线程规则：[StrEnum](https://docs.python.org/3/library/enum.html#enum.StrEnum)、[tomllib](https://docs.python.org/3/library/tomllib.html)、[sqlite3.connect](https://docs.python.org/3/library/sqlite3.html#sqlite3.connect)。
- HTTP 文档与错误结构的依据：[FastAPI OpenAPI 支持](https://fastapi.tiangolo.com/features/)、[RFC 9457](https://www.rfc-editor.org/rfc/rfc9457.html)。

上述资料用于核实架构相关事实；巨潮/披露易私有端点、证券样例、数据分类值和分页仍须用真实样本验证。本文没有把文档修改当作集成测试通过。
