# 财报内容读取、历史比较与基本面分析：后续方案

日期：2026-09-20。状态：需求与路线记录，非详细设计，不纳入一期实现验收。上游：[ARCHITECTURE.md](./ARCHITECTURE.md)。

## 1. 目标与边界

在已经可信归档的报告上，回答三个问题：本期财报具体说了什么；关键数据相对可比历史期间如何变化；这些变化支持哪些基本面观察，还有哪些不确定性。

输出需要同时含结构化数据、趋势计算、文字说明及原文证据。先支持单公司历史纵向分析，再考虑同行横向对比。暂不做估值、股价预测、买卖建议或未经校准的综合评分；银行、保险等行业需要单独的指标模板，不能直接套用普通工商企业模板。

## 2. 建议处理链路

```text
原文及文件版本
  → 文本 / 表格 / 官方结构化事实
  → 科目映射与口径标准化
  → 质量校验与待复核项
  → 可比历史序列与指标计算
  → 基本面观察 + 证据 + 局限
```

每步保存输入版本、输出版本和处理器版本，可单独重跑。解析失败不回滚已下载原文；内容分析依赖归档层的 report_id、artifact_id、checksum，不反向耦合到各市场下载适配器。

## 3. 如何读取具体内容

| 来源形态 | 优先路径 | 必须保留的信息 |
|---|---|---|
| SEC HTML / Inline XBRL | 优先解析 XBRL facts/context/unit；官方 Company Facts 可用于发现、对照和加速；正文另提取管理层讨论及附注 | accession、标签、context、unit、期间、来源版本、原文锚点 |
| 文本型 PDF | 先提取带页码/坐标的文字与表格，识别三大报表、单位行、列头、附注；跨页表格合并须复核 | 页码、区域、表名、行列、原文数值及单位 |
| 扫描 PDF 或混合页 | 逐页判断，必要页 OCR，保留识别质量；不要因部分页扫描就对全文强制 OCR | 页图位置、OCR 结果、工具版本、人工修正轨迹 |
| 附件承载的报表 | 先识别申报与附件关系，再对附件应用上述处理 | 母申报、附件名/角色、各自 checksum |

SEC 官方 API 的聚合事实覆盖有边界，不能假定其包含企业自定义标签及所有分部维度；不足时回到申报文件核验。[SEC EDGAR APIs](https://www.sec.gov/search-filings/edgar-application-programming-interfaces)

大模型可辅助识别表头、科目同义词、附注和撰写解释，但抽取值必须有原文定位。计算使用确定性代码和 Decimal；模型不能补造缺失数字。置信度来自校验结果、证据和抽取质量，不能只采信模型自报分数。

## 4. 为每条指标保留什么

这是概念字段清单，不是现在就要建的数据库表。

- 主体：稳定 issuer_id、证券别名、合并/母公司/分部范围；同公司多地上市不能只按股票代码拼历史。
- 指标：标准 metric_key、原始标签、原始科目名、数值、原始单位/缩放、币种、会计准则；金额用十进制，缺失用 null，不能当 0。
- 时间：instant 或 duration、period_start/end、财年/财季、单季或累计、比较期间标签；允许 52/53 周及财年变更。
- 版本：披露日期/时间及精度、原报/修订/比较期重述、数据获得时间、解析器/规则版本。
- 证据：report_id、artifact_id、checksum、页码/坐标或 HTML/XBRL context 定位、原文片段及必要表头/单位。
- 质量：observed/derived、来源 fact_ids、推导公式、校验状态、冲突和人工复核记录。

同一个历史期间在后来的报告中可能再次披露且数值改变。原始披露值、最新可比重述值都保留，查询时显式选择 `as_reported` 或 `latest_restated`；如做历史时点研究，还需 `as_of` 限定当时已公开信息，不能混用未来信息。

## 5. 如何比较历史

先确认同主体、同口径、同币种/单位、同报表范围、同持续经营范围及相近期间长度，再计算。无法对齐时返回不可比原因，不能强制输出百分比。

| 场景 | 处理原则 |
|---|---|
| 同比 | 流量指标比较去年对应且同长度期间；余额指标区分上年同期末与上年末比较 |
| 环比 | 只比较可比单季或相邻同长度期间；半年累计不能直接对一季度累计 |
| 累计转单季 | 对可加总流量科目：Q2 = H1 − Q1，Q3 = 前三季 − H1，Q4 = FY − 前三季；两端口径/重述版本不一致则不推导 |
| 余额与比率 | 资产、负债、现金余额不能做累计差分；EPS、毛利率、ROE 也不能直接相减得到季度值 |
| TTM | 用四个已核实且不重叠的单季，或可比 FY + 本期累计 − 上年同期累计；缺项不给完整 TTM |
| 零值和负值 | 正基数可用 `(本期−上期)/上期`；零值不给增幅，负值优先呈现绝对变化及扭亏/转亏，避免误导性增长率 |
| 比率变化 | 毛利率 20% → 23% 是增加 3 个百分点；数值增长率另列 |
| 币种与准则变化 | 先标注断点，首版不做隐含汇率转换或把 US GAAP/IFRS/中国准则指标直接视为等价 |

示例：若同口径 H1 营收为 220、Q1 为 100，则推导 Q2 为 120；若上年可比 Q2 为 110，同比约 9.09%。该结论要能追到三条原始事实，标明 Q2 为派生值。若 H1 中比较期已重述，不能直接拿旧 Q1 相减。

获取默认 4 份报告通常不足以做稳健的长期比较。后续分析应按所需指标和期间制定补取计划，例如 3–5 年年报及 8–12 个季度可比数据；港股只有半年度时保持半年度，不拼造季度。具体深度在试点后确定，不增加一期默认下载量。

## 6. 基本面分析首版输出

| 维度 | 可先支持的观察 | 解释边界 |
|---|---|---|
| 增长 | 营收、归母净利润、经营现金流的同口径历史变化 | 区分并购、处置、汇率和一次性收益；证据不足只列待核查原因 |
| 盈利能力 | 毛利率、营业利润率、归母净利率；ROE/ROA 在平均余额齐备后计算 | 不混用利润归属口径；行业模板决定指标适用性 |
| 盈利质量 | 利润与经营现金流是否背离，应收/存货变化 | 现金流波动可能有季节性，不能自动归因于财务造假 |
| 偿债与流动性 | 负债结构、现金与有息债务、到期压力 | “现金”是否受限、租赁是否计入债务需明确；金融业另设模板 |
| 资本投入 | 经营现金流、资本开支、自由现金流的变化 | FCF 属定义型指标，明确采用 CFO−现金资本开支及来源科目，不混用收购支出 |
| 披露风险 | 审计意见、持续经营、诉讼、关联交易和重大会计政策变化 | 提供出处与报告日期；文字解读与数字事实分开 |

建议分析结果固定呈现：数据覆盖情况 → 指标变化表 → 有证据的经营观察 → 风险/缺口 → 来源列表。每项判断带证据 ID 和适用口径，结论允许“证据不足”。先采用透明规则和文字解释，暂不把所有公司压成一个基本面分数。

## 7. 路线与验收方向

1. 小样本试点：先选一个市场、一类非金融公司；包含正常、亏损、修订和特殊财年的样本，人工建立可核对事实集，再比较 PDF/XBRL 路线的准确度与成本。
2. 结构化提取：先做少量核心科目和来源回溯，再覆盖三大报表；披露事实与解析结果分开保存。
3. 历史比较：实现口径检查、累计转单季及版本策略；单元样本覆盖重述、负基数、单位变更、缺失值及非日历财年。
4. 基本面输出：确定性指标与规则先行，大模型仅在已验证事实集上生成带证据的解释，保留模型/提示词版本。

进入下一阶段前确定准确率目标、复核工作量、每份报告的处理时间和成本。金额提取必须保留原始精度，推导值按输入精度处理；会计勾稽允许明确的舍入容差。资产=负债+权益、现金流与余额变动（含汇率等调节项）可作质量信号，不能把未知调节项直接判为数据错误。

未来可按异步任务暴露解析、指标查询、比较和分析接口，复用一期任务/错误/证据标识约定；本阶段不冻结 URL、DDL、OCR/LLM 供应商、模型或向量检索方案。

## 8. 一期只需保留的衔接能力

必须有稳定报告/文件版本 ID、来源 URL 与来源键、原文 checksum、UTC 抓取时间、报告期及其可信来源、语言、申报类型、修订信息、可空的来源主体标识。元数据未知就保留未知，不为后续分析强行猜值。

保留原始文件与必要来源字段；解析结果将来单独保存。支持手动扩大 `last_n` 和明确显示历史覆盖缺口即可，一期不提前实现指标库、OCR、大模型调用或分析引擎。

## 9. 参考项目启发：financial-report-minesweeper

2026-09-20 静态审查：[项目](https://github.com/terancejiang/financial-report-minesweeper)，固定提交 `483d3fcd65da32c2de2898ab6f9529ae87e92353`。阅读了数据采集、下载脚本、Skill 流程和规则文件，并抽查共用客户端；未安装、执行第三方脚本或验证其输出准确率。以下是针对本项目的取舍，不代表采用其全部规则。

### 9.1 值得吸收的结构

该项目用 Tushare 取得年度财务数据，以 PDF 补充定性证据，再通过 Skill 指导模型执行检查及输出报告。我们可借鉴量化与附注分工、逐项列出判断依据、保留分析中间结果的组织方式。[流程来源](https://github.com/terancejiang/financial-report-minesweeper/blob/483d3fcd65da32c2de2898ab6f9529ae87e92353/skill/SKILL.md)

建议后续明确三个边界：

1. **ReportProvider**：发现并归档官方原文，沿用一期。
2. **FactProvider**：提供带来源、单位和期间的结构化事实；官方 XBRL、原文提取均可接入。第三方财务 API 仅是后续可选方案，需独立决定，不改变一期官方来源约束。
3. **AnalysisRecipe**：声明需要哪些事实、历史窗口及附注证据；确定性代码完成数值规则，模型辅助语义提取与解释。

三者是职责边界，不要求拆为服务。一期只实现 ReportProvider；后两者待试点后设计。外部结构化数据能减少自行抽表，但仍需做版本、口径和原文核对，不能承诺无误差。

### 9.2 用分析所需证据驱动读取与补取

为每种分析登记所需科目、年数、章节、可接受来源和最低覆盖，先规划缺口，再补取。例如现金流趋势需要连续历史事实；研发资本化需要开发支出附注；审计机构变动需要多期审计信息。一次下载最新 PDF 无法满足所有历史定性比较。

该项目采用全文转文本后交给模型阅读的方式。我们的建议是保留全文和页面映射，先定位所需章节/表格，再提取并验证上下文；找不到时扩大到全文、同义词或 OCR。关键表格需保留表头、单位和跨页关系。检索没命中只能说明本次未找到，不能据此断言没有风险事件。这个流程不以引入向量数据库为前提。

### 9.3 规则必须可执行、可复核且表达适用范围

规则定义至少包括：rule_id/version、适用市场/行业/报表范围、依赖指标/期间、计算方法、阈值来源、缺失处理、证据要求和解释模板。结果分别记录执行状态与风险程度：`evaluated / insufficient_data / not_applicable / error`；仅 evaluated 结果给出具体信号，避免把数据缺失计作正常。

规则文件中的若干表达需要特别避免照搬：用应收周转率替代坏账计提比例进行比较，二者量纲不一致；允许商誉 null 通过会混淆未知和零；带绝对值的投资现金流规则还需先验证流出方向。我们应为每条规则定义前置条件，并用反例检查公式，阈值经行业样本验证后再使用。[被审查的规则文件](https://github.com/terancejiang/financial-report-minesweeper/blob/483d3fcd65da32c2de2898ab6f9529ae87e92353/skill/references/checklist-rules.md)

风险信号不等于已证实违规或造假；“异常”先生成核查问题和证据，不能直接给公司定性。排雷作为基本面分析的一个可选规则组，与增长、盈利能力、现金流、偿债能力分开输出。

### 9.4 时间与数据质量应先于评分

采集入口按 `end_date` 排序后截取若干记录，未先统一各表期间及同期间披露版本；同行取同一行业前 20 家各自最新年度数据。其 CLI 只接受股票代码和历史长度，没有目标报告期/信息截止日参数。因此我们应显式设计 target_period、as_of 和版本选择；按期间键关联三表，不能用各数组的第 0/1 项代表同一年度。[采集入口](https://github.com/terancejiang/financial-report-minesweeper/blob/483d3fcd65da32c2de2898ab6f9529ae87e92353/scripts/minesweeper_data.py)

同行比较后置，并保存选样依据、样本规模、每家期间和排除理由。数据源错误必须保留 error 状态，不能在结果中与正常空集合不可区分。

若未来输出评分，必须同时显示适用规则数、已评估数、缺失的关键证据及覆盖率。覆盖不足时仅列已确认信号，不输出“低风险”或“高置信度”。规则之间相关时要检查重复计分；不把经验得分解释为风险概率。

### 9.5 一期获取服务仍按既定方案实现

参考项目依靠搜索找到 URL 后下载 PDF；下载脚本同名目标会先删除再改名，没有我们设计的来源索引和内容版本机制。这适合作为人工辅助流程参考，不能直接承接标准 HTTP 获取服务。[下载流程](https://github.com/terancejiang/financial-report-minesweeper/blob/483d3fcd65da32c2de2898ab6f9529ae87e92353/skill/download-report.md)、[下载脚本](https://github.com/terancejiang/financial-report-minesweeper/blob/483d3fcd65da32c2de2898ab6f9529ae87e92353/scripts/download_report.py)

因此继续采用官方来源适配器、稳定来源键、任务幂等与文件版本。后续分析每次生成独立 analysis_run_id，固定输入 artifact/fact 快照、目标期间、规则/解析器/模型版本；结构化结果为主，Markdown 报告为其展示产物，多次运行不能覆盖之前的证据。

建议试点先选少量有明确输入和证据的观察：营收/利润趋势、利润与现金流匹配、应收与存货变化、债务与现金结构、审计及重大附注事项。先人工核验每项结果与缺失场景，再扩规则数量和行业范围。

## 10. 参考 Skill 启发：DB-GPT financial-report-analyzer

2026-09-20 静态审查：[Skill](https://github.com/eosphoros-ai/DB-GPT/blob/ca9f014cb3ead157ca2ee6ce645658f5fb55138c/skills/financial-report-analyzer/SKILL.md)，固定提交 `ca9f014cb3ead157ca2ee6ce645658f5fb55138c`。阅读了三个配套脚本、指标/分析参考和报告模板，未安装 DB-GPT、执行脚本或验证真实 PDF 准确率。审查范围为该 Skill，不对整个 DB-GPT 项目的能力作判断。

### 10.1 适合借鉴的分析分工

该 Skill 将提取、比率计算、图表生成、文字分析和模板渲染分开，并让宿主保存中间数据，再与模型生成的解释合并。这为基本面报告的组织提供了参考。我们的方案应保留这种职责分工，通过明确输入/输出和持久化产物衔接，不依赖特定宿主的 react_state 或工具名。

建议后续形成如下处理路径；这是概念责任划分，不是现在创建模块或冻结详细接口：

```text
已归档原文 / 官方结构化数据
  → 带期间、单位、口径和证据的事实
  → 校验及可比历史序列
  → 一次计算的指标结果
  → 基本面观察 / 可选风险规则
  → 受证据约束的文字解释
  → 同一分析结果的 JSON、图表与报告展示
```

模型不重新填写指标或图表数值，只生成关联 metric_ids/evidence_ids 的解释。文字可以说明管理层披露的原因或待核查假设，不能仅由指标变动推出未披露的经营原因。API 应提供分析数据与证据，HTML/Markdown 是展示产物，不把模板占位符或 HTML 标签作为业务数据模型。

### 10.2 提取能力应按实际实现评估

[extract_financials.py](https://github.com/eosphoros-ai/DB-GPT/blob/ca9f014cb3ead157ca2ee6ce645658f5fb55138c/skills/financial-report-analyzer/scripts/extract_financials.py) 将 PDF 逐页转文本并合并，以正则选取科目后首次出现的数字；没有保留表格列、页面定位、币种/缩放与报表范围。

从代码可推导以下风险：负号或括号可能被跳过，只留下正数；表头年份可能被当金额；金额以“万元”披露时也没有缩放；同名科目的上期值、母公司值或附注值可能误入本期。现有输出是每科目一个标量，没有生成 prev_* 或三年序列。因此该脚本只能作为原型思路，不能据 Skill 的能力描述认定已完成可靠历史提取。

后续验收必须有负数/括号、单位行、双期间列、合并与母公司并列、重复科目、跨页表和扫描页反例。对“提取成功”应要求值、期间、单位、范围和证据同时正确；只有某个字段非空不等于该字段可信。

### 10.3 指标定义必须与代码和输入口径一致

[calculate_ratios.py](https://github.com/eosphoros-ai/DB-GPT/blob/ca9f014cb3ead157ca2ee6ce645658f5fb55138c/skills/financial-report-analyzer/scripts/calculate_ratios.py) 用单一 equity 计算 ROE，而[指标参考](https://github.com/eosphoros-ai/DB-GPT/blob/ca9f014cb3ead157ca2ee6ce645658f5fb55138c/skills/financial-report-analyzer/references/financial_metrics.md) 写的是平均净资产；默认提取链路也未提供扣非净利润和历史值，相关同比及行业对照不能自动得到。格式化字符串与指标计算放在一起，还不适合作为通用事实接口。

我们的指标需保留 metric_key、definition_version、输入 fact_ids、计算期间/范围、原始十进制值、单位及状态；展示文本另生成。ROE 必须说明平均/加权平均/期末净资产定义和利润归属，缺平均余额时不冒充标准定义。营业总成本不能作为营业成本的无条件替代项；指标库需区分不同科目，而不是为凑齐公式自动选近似名称。

“报告期间”“披露时间”“分析生成时间”分别建字段；不把其中一个日期复用为另一个标签。行业均值或比较数据未取得时保持不可用，不让模型填充估计数。

### 10.4 图表与正文必须共享真实结果

[generate_charts.py](https://github.com/eosphoros-ai/DB-GPT/blob/ca9f014cb3ead157ca2ee6ce645658f5fb55138c/skills/financial-report-analyzer/scripts/generate_charts.py) 有独立比率计算、部分缺失转零、净现比数值截至 200%、比例图横轴从零开始等处理。这可能使图表与正文的 N/A 不一致，掩盖缺失、极值或负值。图表不能以外观完整替代数据完整。

我们的呈现约束：

- 图表读取统一指标结果，不再次实现财务公式；缺失显示断点或不可用说明，不画成零。
- 负利润、负净资产及负比率必须可表达；不适合饼图时换表格/条形图，不能强行渲染。
- 如限制显示范围，必须标注越界与真实值；不能把截断后的显示数值标成实际值。
- 金额标注实际币种、缩放和期间；期间流量与时点余额分图或明确区分。
- 优先绘制同口径历史趋势、利润与现金流对照，再按完整度选择结构图；图表缺失不导致已有分析结果丢失。
- 产物按 analysis_run_id 隔离；模板只渲染经校验的数据和安全文本，证据可回溯到文件版本。

### 10.5 两个参考的组合方式

| 参考 | 吸收的思路 | 本项目仍需解决 |
|---|---|---|
| financial-report-minesweeper | 问题清单、风险信号、结构化数据与附注交叉验证 | 期间/口径对齐、规则适用性、覆盖不足与风险分开 |
| DB-GPT financial-report-analyzer | 分步计算与解释、图表和固定报告结构 | 可靠提取、统一指标源、展示一致性和宿主解耦 |
| 本项目一期 | 官方原文获取、版本与来源证据、HTTP 获取入口 | 后续消费这些证据，不提前扩大为完整分析平台 |

下一阶段先完成少量关键事实和历史对比，再验证少量规则；最后增加图表与报告模板。基础数据未通过质量检查时，可以输出缺口和待复核证据，不生成看似完整的基本面评价。
